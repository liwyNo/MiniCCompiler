记录下需要写的东西，需要注意的东西
1. 符号表 大体完成，处理 p 开头的变量时，要注意先改名，用 extend xxx，第一遍预处理的时候就都把 p 打头的名字的变量改好
2. 处理表达式，顺便给变量搞出来活跃区间
3. 新搞一个 analysis.cpp 模块，分析变量活跃性，注意以一个函数为单位
4. 然后就可以分配寄存器，注意以一个函数为单位 ，特判数字，数字单独保留出来一个寄存器，假如是两个数字运算，就先多加一句话
5. 产出代码

6. 寄存器分配不优，应先选择没有被钦定的溢出，否则还得恢复一下
7. 浪费了2个寄存器用来存数字，太浪费
8. 其实最好的是等式分成两边，先搞右边的分配，然后就没右边的事了，然后可以接着给左边的用，但是这个太难写了
9. 作为等式左边的变量，其实分配寄存器时不用给他 Load，但是这会很难写，因为可能 t1 = t1 + t2，这样可能第二个 t1 就忘了放进来
10. 等式左边的变量 ，分配寄存器时不用给那个寄存器 load 它的值 fixed!
11. 只把值不在内存内的溢出时要从寄存器里存到内存里 fixed!